POLYMARKET_PRIVATE_KEY=your_private_key_here
CAPITAL_USD=50
DRY_RUN=true # true for testing, false for real trading

# Polygon RPC endpoint (explicit > POLYGON_RPC_URL > public fallback).
# Set this to your own node (e.g. Alchemy/Infura/QuickNode) — the public
# endpoint is rate-limited and unreliable for live trading.
POLYGON_RPC_URL=https://polygon-rpc.com

# RISK MANAGEMENT (optional — defaults shown; fractions of CAPITAL_USD)
DAILY_MAX_LOSS_PCT=0.05
MONTHLY_MAX_LOSS_PCT=0.15
MAX_DRAWDOWN_PCT=0.25
TOTAL_MAX_LOSS_PCT=0.40

# DASHBOARD SECURITY
# Bind address for the dashboard server (default 127.0.0.1 = localhost only;
# set 0.0.0.0 only if you must reach it from other machines)
# DASHBOARD_HOST=127.0.0.1
# Shared secret required on /api/* and the WebSocket (it carries trading
# commands, so auth is ALWAYS on). If unset, a random token is generated on
# every start and the access URL (with ?token=...) is printed once.
# Set your own to keep a stable URL: openssl rand -hex 24
# DASHBOARD_TOKEN=generate-a-long-random-string

# STRATEGIES CONFIG false as defualt
ARBITRAGE_ENABLED=false
DIPARB_ENABLED=false
SMARTMONEY_ENABLED=false
TREND_ANALYSIS_ENABLED=false

# EXTRA WALLETS TO COPY-TRADE (optional, comma-separated)
# Followed in addition to the leaderboard. Empty by default: nothing is copied
# unless you chose it. Each wallet must still pass the bot's quality gates
# (win rate, profit factor, consistency, min PnL/trades) — rejected wallets are
# logged as "Custom wallet rejected" and NOT followed. Only add wallets you have
# vetted yourself. Invalid entries are reported at startup, never silently used.
# CUSTOM_WALLETS=0xabc...,0xdef...
